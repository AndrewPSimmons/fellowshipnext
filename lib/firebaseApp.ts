// import { credential, remoteConfig, initializeApp } from 'firebase-admin';


// import key from './firebaseServiceKey';


// // const initFirebaseApp = () => {
// //     initializeApp({
// //         credential: credential.cert(key as any),
// //     });
// // }
// //   credential: credential.cert(key as any)
// //   databaseURL: 'https://<DATABASE_NAME>.firebaseio.com'

// export const app = initializeApp({
//     credential: credential.cert(key as any),
// });
// export const remoteConfigInit = remoteConfig(app);

// export const fireStoreConfig =  remoteConfig(app).getTemplate();

export default {}